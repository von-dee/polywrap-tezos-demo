{
  "name": "@web3api/harbinger-wrapper-example",
  "homepage": "https://von-dee.github.io/polywrap-tezos-demo/harbinger-demo/",
  "description": "Example of harbinger wrapper",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@web3api/client-js": "0.0.1-prealpha.61",
    "gh-pages": "^3.2.3",
    "react": "^16.10.2",
    "react-dom": "^16.10.2",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "start": "react-scripts --openssl-legacy-provider start",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
